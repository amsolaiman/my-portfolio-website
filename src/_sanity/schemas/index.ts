//
import content from './content';
import experience from './experience';
import projects from './projects';

// ----------------------------------------------------------------------

const schemas = [content, experience, projects];

export default schemas;
