// ----------------------------------------------------------------------

export const PATHS = {
  root: '/',
  studio: '/studio',
  api: {
    root: '/api',
    revalidate: '/api/revalidate',
  },
};
